extends layout

block content
    .row
        each user in users
            .col-6.col-lg-6
                h4.text-primary= 'Firstname : ' + user.name
                p= 'Email : ' + user.email
                p= 'Password : ' + user.password
                hr
